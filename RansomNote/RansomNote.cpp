#include<iostream>
#include<string.h>
using namespace std;

int searchWord(string input, string* dictionary, )
inline int ransomNote(string* noteWords, string* dictionary, int numOfWordsInNote, int numOfWordsInDict)
{
	
}
int main()
{  int numOfWordsInNote, numOfWordsInDict, i ;
   cin>>numOfWordsInDict ;
   cin>>numOfWordsInNote ;
   
   string noteWords[numOfWordsInNote], dictionary[numOfWordsInDict] ;
   
   for(i=0 ; i<numOfWordsInDict ; i++)
   {
      fflush(stdin);
      cin>>dictionary[i];
   }
   
   for(i=0 ; i<numOfWordsInDict ; i++)
   {
      fflush(stdin);
      cin>>dictionary[i];
   }
   
   result = ransomNote(noteWords, dictionary, numOfWordsInNote, numOfWordsInDict)
   cout<<noteWords[0] ;
   return 0;
}
